services:
  api:
    build: .
    container_name: fastapi-api

    ports:
      - "8000:8000"

    depends_on:
      - redis

    environment:
      REDIS_HOST: redis

  redis:
    image: redis:7-alpine
    container_name: redis-server
